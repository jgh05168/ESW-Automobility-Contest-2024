# ============================================================================
# GENERATED BY PARA, PopcornSAR
# Copyright(c) 2024 by PopcornSAR Co.,Ltd. All rights reserved.
# ============================================================================
 
cmake_minimum_required(VERSION 3.16)
 
project(Machine)
 
# ============================================================================
# Do NOT modify the section below.
# This setting is required only once immediately after the project command in
# the top-level CMakeLists.txt.
# ============================================================================
include(${CMAKE_SOURCE_DIR}/cmake/ParaSdk.cmake)
# ============================================================================
 
add_subdirectory(CM)
add_subdirectory(Camera)
add_subdirectory(EM)
add_subdirectory(Inference)
add_subdirectory(Lidar)
add_subdirectory(Navigate)
add_subdirectory(SM)
add_subdirectory(SensorFusion)
add_subdirectory(Servo)
add_subdirectory(Simulator)

find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
target_link_libraries(Camera PRIVATE ${OpenCV_LIBS})

target_include_directories(Lidar PRIVATE /home/deepracer/rplidar_sdk/sdk/include/)
target_link_libraries(Lidar PRIVATE /home/deepracer/rplidar_sdk/output/Linux/Release/libsl_lidar_sdk.a)